# BookOasis Mate

**FF용 플러그인이며 BookOasis 서버가 실행되는 파일시스템에 접근할 수 있어야 합니다.**

## 주요 기능

- BookOasis 라이브러리 상태 요약과 문제 도서 진단.
- SQLite·MariaDB를 선택해 일반·성인·오디오북 DB 상태와 스캔 작업 확인.
- 스캔 상태 및 주요 로그 실시간 확인.
- 시리즈 누락, 표지 문제와 고아 표지파일 검사.
- 안전한 읽기 전용 SQL 진단 도구와 운영 프리셋.
- gd-poller 변경 감지 기반 rclone VFS 갱신과 BookOasis 증분 스캔.
- BookOasis 카테고리 내보내기·가져오기.
- Kavita 및 BookOasis 공유 패키지 통DB 이관.
- BookOasis 커스텀 폰트 업로드와 설치 상태 확인.

## DB 엔진 설정

- BookOasis v1.8 계열은 설정에서 `SQLite`, `MariaDB`, `자동 감지`를 선택할 수 있습니다.
- MariaDB 모드는 `PyMySQL 1.1` 이상이 필요하며 호스트·포트·사용자·비밀번호·DB 접두어를 입력합니다.
- 자동 감지는 FF의 `DB_ENGINE`, `DBMS` 환경변수와 MariaDB 설정을 확인합니다. 연결 실패 시 남아 있는 SQLite 파일로 자동 전환하지 않습니다.
- 상태 요약, 문제 도서, 스캔 상태, 시리즈 누락, 표지 검사, 고아 표지 정리, Google Drive 연동과 읽기 전용 SQL은 두 엔진을 지원합니다.
- Mate의 통DB 패키지 이관은 SQLite DB 파일 교체 방식이므로 MariaDB에서는 실행하지 않습니다. BookOasis 공식 이관 도구를 사용해야 합니다.
- Mate 내 카테고리 패키지 이관도 현재 SQLite 대상으로 제한됩니다. MariaDB 카테고리 이관은 BookOasis v1.8.7 이상의 공식 `tools/export_category.py`, `tools/import_category.py`를 사용합니다.

## Changelog

- v1.3.0 (2026-08-08)
  - SQLite·MariaDB 이중 엔진 지원
    - 설정에서 DB 엔진을 명시적으로 선택하거나 BookOasis 환경변수를 기준으로 자동 감지하도록 추가.
    - 일반·성인·오디오북 진단, 보관함 조회, 스캔 상태, 시리즈 누락과 표지 참조 조회를 공통 읽기 전용 DB 어댑터로 전환.
    - MariaDB의 `%s` placeholder, dict row, `utf8mb4_bin` 환경과 날짜·Decimal 결과 정규화 반영.
  - 운영 안전성
    - MariaDB 연결 실패 시 오래된 SQLite 파일로 조용히 전환하지 않도록 원본 엔진을 명확히 표시.
    - 표지 검사, 고아 표지 정리와 일괄 재스캔 작업의 MariaDB 설정 파일을 일회성·제한 권한으로 처리.
    - SQL 도구에 MariaDB 제한 시간과 민감 테이블·시스템 스키마 차단 정책 적용.
    - SQLite 파일 교체 방식의 카테고리·통DB 이관을 MariaDB에서 차단하고 공식 도구 안내 추가.

- v1.2.0 (2026-08-04)
  - 오디오북 대응
    - `media_audiobook.db` 상태 요약, DB 정보, 읽기 전용 SQL과 스캔 상태 지원.
    - Google Drive 변경 감지에서 오디오 파일과 오디오북 보관함 경로 인식 지원.
    - 도서 전용 진단·메타데이터 작업에는 오디오북을 노출하지 않도록 범위 분리.
  - 커스텀 폰트
    - BookOasis 커스텀 폰트 디렉터리에 TTF·OTF·WOFF·WOFF2 파일을 안전하게 업로드하는 설정 UI 추가.
    - 허용 형식·파일 헤더·경로를 검증하고 설치 목록과 도커 재시작 안내 제공.
  - 상세 보기
    - BookOasis v1.6.4의 표준 난독화 딥링크와 레거시 폴백 계약 반영.
    - 일반·성인 DB 모두 상세 페이지를 새 탭에서 복원하도록 이전 차단 정책 제거.
  - 배포 구성
    - 공개 저장소에서 로컬 테스트 소스와 개발 기록을 제외하도록 정리.

- v1.1.0 (2026-08-03)
  - BookOasis 연동
    - 최신 관리자 API의 플러그인 상태·업데이트 정보와 개별 도서 작업 계약 반영.
    - 난독화된 도서 상세 주소 체계 지원과 상세 보기 호환성 개선.
    - 최신 스캐너 상태, 원격 도서 페이지 분석 제한과 gd-poller 변경 감지 기준 반영.
  - 라이브러리 진단
    - 일반·성인 DB를 안전하게 조회하는 읽기 전용 SQL 도구와 사용자·권한·진행률 진단 프리셋 추가.
    - 스캔 상태 표시와 장기 미스캔·원격 보관함 진단 기준 개선.
  - 도서 재스캔
    - 문제 도서와 표지 검사 결과의 백그라운드 일괄 재스캔 추가.
    - 일시적인 API 연결 중단을 항목별 재시도하고 화면별 작업 상태를 분리해 표시하도록 안정성 개선.
    - 결과 행 메뉴의 개별 도서 재스캔 복원과 일괄 작업 중 중복 실행 방지.
  - 표지 검사
    - 저해상도·작은 파일·비정상 비율 검사를 웹 요청과 분리된 백그라운드 작업으로 전환.
    - 한 번의 파일 분석 결과를 세 검사 유형이 공유하고 메뉴 재진입 시 진행률과 결과를 복원하도록 개선.
    - 표지 없음 검사는 빠른 DB 조회 방식을 유지하고 정밀 검사 결과를 일괄 재스캔과 연동.
- v1.0.2 (2026-07-29)
  - 고아 표지파일 정리와 카테고리 이관을 별도 프로세스로 분리해 장시간 작업 중 FF 응답성을 개선.
  - BookOasis URL, 스캔 웹훅 토큰과 API 제한 시간 안내를 보완.
  - 커스텀 표지 디렉터리 설정과 경로 선택 기능 추가.
  - 기존 카테고리 병합 시 불필요한 신규 카테고리 이름 입력 제거.
- v1.0.1 (2026-07-29)
  - 통DB 이관 매뉴얼의 Kavita 이관 절차를 현재 동작에 맞게 수정.
  - README 플러그인 설명을 FF 표기로 정리.
- v1.0.0 (2026-07-29)
  - 최초 공개.

# BookOasis Mate

현재 버전은 **v1.11.3**입니다.

**FF용 플러그인이며 BookOasis 서버가 실행되는 파일시스템에 접근할 수 있어야 합니다.**

## 주요 기능

- BookOasis 라이브러리 상태 요약과 문제 도서 진단.
- SQLite·MariaDB를 선택해 일반·성인·오디오북·비디오 DB 상태와 스캔 작업 확인.
- 스캔 상태 및 주요 로그 실시간 확인.
- 시리즈 누락, 표지 문제와 고아 표지파일 검사.
- 안전한 읽기 전용 SQL 진단 도구와 SQLite·MariaDB 관리자 진단 프리셋.
- gd-poller CommandDispatcher·WebhookDispatcher 또는 Mate에 직접 지정한 rclone 실행 파일·설정을 사용하는 Google Drive·Union upstream 변경 감지 기반 BookOasis 개별 경로 스캔.
- 로컬 폴더 감지. Linux 로컬 디스크는 watchdog, Windows Docker·CIFS/NFS·FUSE는 폴링으로 확인하고 별도 이벤트 대기열에서 스캔합니다.
- BookOasis 일반·성인·오디오북·비디오 카테고리 내보내기·가져오기.
- Kavita 및 BookOasis 공유 패키지 통DB 이관.
- BookOasis 커스텀 폰트 업로드와 설치 상태 확인.
- GitHub·비공개 Gitea 카탈로그를 포함한 BookOasis 네이티브 플러그인 설치·업데이트·삭제와 설정 계약 관리.
- BookOasis `.env`·Docker Compose 안전 편집과 BookOasis 단일 서비스 Docker 재시작·구성 적용·업데이트 관리.
- GHCR 이미지 업데이트 감지, Docker 작업 실시간 로그와 Ubuntu·Synology 호스트 Docker 자동 호환.
- 설정 화면에서 MariaDB 필수 구성요소 상태 확인 및 필요한 패키지만 선택 설치.

## Linux 호스트 진단 스크립트

NAS·Ubuntu 등 **Docker 호스트**에서 실행합니다. Python 3.8 이상과 Docker CLI 접근 권한이 필요하며, 원격 한 줄 실행에는 Bash·curl도 필요합니다. 추가 Python 패키지는 설치하지 않습니다.

```bash
python3 mate_diagnose.py
# 보고서 저장 위치 지정
python3 mate_diagnose.py --output-dir ./mate-reports
```

저장소 루트에서 실행할 때는 `bash scripts/mate_diagnose.sh` 또는 `python3 scripts/mate_diagnose.py`를 사용하세요. 셸 실행기는 Python 3.8+·Docker를 확인하고 같은 폴더의 본체를 실행합니다. 컨테이너와 증상을 번호로 선택하며, 선택 경로는 예시를 참고해 직접 입력하거나 Enter로 건너뜁니다. 자료 경로는 Mate에서는 `/host/volume2/DATA/02.Ebook`, BookOasis에서는 `/volume2/DATA/02.Ebook`처럼 다를 수 있습니다. 예시는 자동 입력하지 않습니다.

화면에는 6단계 진행과 짧은 상태·다음 조치만 표시합니다. 상세 JSON·마운트·설정 근거는 TXT에만 저장하고, 마지막에 요약과 결과 파일 위치를 표시합니다. Docker 권한이 없을 때 자동 sudo나 패키지 설치를 하지 않습니다.

아래 명령은 v1.11.0이 **main에 병합된 후** 사용하는 기본 실행 명령입니다. 원격 코드를 실행하므로 신뢰하는 저장소인지 확인하세요.

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/colaiuta77/bookoasis_mate/main/scripts/mate_diagnose.sh)"
```

셸 실행기는 검증된 커밋 `69c3236aae4e3c9723caaf8bbe23935fd63f82fc`의 Python 진단 v1.1을 임시 파일로 받아 실행하고 임시 파일만 정리합니다. `main` 실행기 자체도 고정하려면 위 URL의 `main`을 원하는 릴리스 커밋으로 바꾸세요. 같은 폴더에 본체가 있는 로컬 실행은 해당 파일을 우선합니다.

- 컨테이너 상태·이미지 식별값, Python·의존성, Mate 버전, 마운트·경로 접근, Mate 허용 설정과 최신 최대 5,000건 이벤트 상태를 확인합니다.
- 접속 검사는 인증 없이 URL 원점 `/`에 GET 1회만 요청합니다. 리디렉션·로그인·스캔을 실행하지 않으며 화면 지연의 원인을 확정하는 검사는 아닙니다.
- 최근 1시간의 Docker 로그 최대 300줄에서 알려진 오류 패턴을 분류합니다. 원문 대신 유형·건수·확인 방법만 보고합니다. 파일에만 기록된 로그는 포함되지 않습니다.
- 추가 SQLite `quick_check`는 선택 사항입니다. 읽기 전용으로 실행하지만 부하가 생길 수 있습니다. 검사별 시간 제한으로 건너뛰며 커널에서 멈춘 네트워크 I/O는 즉시 종료되지 않을 수 있습니다.
- 결과는 `mate-diagnosis-날짜-시간.txt`로 저장합니다. 정상·주의·오류·확인 불가를 구분하며, 부분 실패·중단도 보고서에 남깁니다. Linux 보고서 권한은 소유자 읽기/쓰기 전용입니다.

비밀번호·환경변수·전체 설정·원문 로그는 보고서에 넣지 않습니다. **컨테이너 이름·자료 경로·사용자 입력 설명은 공유 전 확인하세요.** 자동 업로드·서비스 재시작·설정 변경·DB 복구는 하지 않습니다. 실제 앱 사용자와 `docker exec` 사용자의 권한은 다를 수 있습니다.

이 보고서는 초기 분류용입니다. Drive 실제 이벤트 API·Gitea 인증·MariaDB 내부 상태·Redis 데이터·브라우저 성능은 직접 검사하지 않으며 필요 시 별도 증거를 요청합니다.

## Docker 관리 사용 조건

Docker 관리 기능은 FlaskFarm 컨테이너에서 호스트 Docker CLI·Compose와 BookOasis 경로에 접근해야 합니다. FlaskFarm의 Docker Compose 설정에 호스트 루트를 `/host`로 마운트하세요.

```yaml
volumes:
  - /:/host
```

`/host` 마운트가 없으면 호스트 Docker CLI와 Compose, BookOasis Compose 경로를 확인할 수 없어 Docker 관리 기능을 사용할 수 없습니다.

## DB 엔진 설정

- BookOasis Mate v1.11.0은 설정에서 `SQLite`, `MariaDB`, `자동 감지`를 선택할 수 있습니다.
- MariaDB 모드는 `PyMySQL 1.1` 이상이 필요하며 호스트·포트·사용자·비밀번호·DB 접두어를 입력합니다.
- 자동 감지는 FF의 `DB_ENGINE`, `DBMS` 환경변수와 MariaDB 설정을 확인합니다. 연결 실패 시 남아 있는 SQLite 파일로 자동 전환하지 않습니다.
- 상태 요약, 문제 도서, 스캔 상태, 시리즈 누락, 표지 검사, 고아 표지 정리, Google Drive 연동과 읽기 전용 SQL은 두 엔진을 지원합니다.
- 카테고리 패키지 이관은 공식 2.0 데이터 형식을 사용하며 SQLite와 MariaDB에서 내보내기·신규 가져오기·기존 카테고리 병합을 지원합니다.
- 오디오북 카테고리 패키지는 오디오북·트랙·재생 진행률·포스터를 포함하며 대상 오디오북 DB의 사용자 ID가 일치하는 진행률만 복원합니다.
- 통DB 이관은 SQLite 파일 패키지와 MariaDB 논리 백업 패키지를 엔진별로 지원합니다. MariaDB에서는 FF 컨테이너에 `mariadb-dump`와 `mariadb` 또는 호환 `mysql` 클라이언트가 필요합니다.
- SQLite와 MariaDB 통DB 패키지는 서로 교차 복원할 수 없습니다.

## 플러그인 셸 스크립트 정책

- `.sh`는 기본 차단합니다. 서버의 검증·신뢰 카탈로그 항목은 포함을 허용합니다.
- 직접 입력한 GitHub URL은 경고 확인과 이번 검사·설치에 대한 명시 동의가 있어야 `.sh`를 허용합니다. 동의는 다음 검사에 자동 적용하지 않습니다. 직접 입력한 Gitea·ZIP에는 적용하지 않습니다.
- 허용 여부는 최종 설치 재검증까지 유지하며 Mate가 `.sh`를 직접 실행하지는 않습니다. 경로 이탈·심볼릭 링크·다른 위험 확장자 검사는 유지합니다. 플러그인 Python 코드는 실행 권한이 있으므로 저장소 자체를 신뢰할 때만 설치하세요.

## 로컬 폴더 감지

- 경로당 감시 항목 한도는 설정에서 기본 200,000~최대 1,200,000으로 조정합니다. 책 수가 아닌 감시 확장자 파일과 폴더 합계이며 비대상 파일·제외 경로는 기준에서 제외합니다. `.bookoasisignore`는 별도 포함합니다. 한도·확장자 변경은 작업 중지·저장·새 기준 시작 순서로 적용합니다.
- 실시간은 최초 전체 수집 후 변경 파일·폴더 범위만 비교하고 DB 저장 확인 후 기준을 갱신합니다. 1시간마다 또는 변경 경로 10,000개 초과 시 전체 비교로 신호 누락을 보완합니다. 폴링은 기존처럼 전체 비교합니다. 마운트·대량 삭제 보호는 유지합니다.
- 60만 권도 폴더와 메타데이터 때문에 100만 항목을 초과할 수 있습니다. 최초/정기 전체 비교는 여전히 메모리·I/O 비용이 있으며, Linux inotify 한도는 별개입니다. 폴더가 많은 경우 OS 감시 한도를 운영자가 확인해야 하며 자동 변경하지 않습니다.

- 실시간 감지 구성요소 카드에서 watchdog 상태를 확인하고 설치·재설치할 수 있습니다. 감지 중에는 설치할 수 없으며 현재 FlaskFarm Python의 pip를 사용합니다. 폴링에는 필요하지 않습니다.
- 변경 이벤트의 JSON 버튼은 상세 기록을 표시하고 삭제 버튼은 완료·최종 실패 기록만 삭제합니다. 실제 파일에는 영향을 주지 않습니다.
- 이벤트 DB 자동 삭제는 기본 On·30일이며 1~3650일로 설정할 수 있습니다. 감지 작업자가 실행 중이면 새 이벤트가 없어도 매시간 종료 기록을 정리합니다. 대기·재시도·처리 중 기록은 유지합니다.

- 로컬 알림은 로컬 감지 설정의 Discord 웹훅으로만 전송합니다. 빈 주소는 알림을 끄며 기존 Google Drive 웹훅은 자동 복사하지 않습니다.

- 감지 제외 패턴을 한 줄에 하나씩 설정할 수 있습니다. 기본값은 `@eaDir/`, `#recycle/`이며 폴더 하위 스냅샷 탐색도 제외합니다. `*.tmp`, `*.sample.cbz` 같은 파일 패턴도 지원합니다. 작업 중지 후 저장하고 새 기준으로 시작해야 적용되며, 기존 이벤트·대기열은 유지합니다. BookOasis의 스캔 제외 설정과는 별개입니다.

- FlaskFarm과 BookOasis가 동일 자료에 접근할 수 있도록 마운트하고, `로컬 폴더 감지` 메뉴에 양쪽 컨테이너 경로를 지정합니다.
- Linux 로컬 실시간 감지는 `watchdog>=4,<7`이 필요합니다. 폴링은 추가 패키지 없이 사용하며 기본 주기는 300초입니다.
- Windows Docker는 컨테이너 경로로 폴링합니다. NAS에서 Windows PC를 감시하려면 SMB 공유를 CIFS로 마운트하세요. Windows 네이티브 FlaskFarm은 지원 범위 밖입니다.
- 최초 실행은 기준만 저장합니다. 이후 '이어서 시작' 및 활성화 상태의 자동 시작은 저장된 기준과 전체 비교해 중지 중 변경을 수집합니다. 이동은 삭제+생성으로 표시합니다.
- '새 기준으로 시작'은 성공한 전체 수집 후 저장 기준을 교체하며 중지 중 변경을 처리하지 않습니다. 기준은 Mate SQLite DB 옆 `bookoasis_mate_local_baseline.db`에 변경분만 저장합니다. 이 데이터 볼륨을 유지하세요. 이벤트 기록 자동 삭제는 기준 DB를 지우지 않습니다.
- 재시작 시 전체 조회와 inotify 재등록은 여전히 필요합니다. 설정·루트 식별 불일치, 조회 실패와 대량 삭제는 보류합니다. 큐 저장과 기준 저장 사이 종료 시 일부 이벤트가 재전달될 수 있습니다.
- 조회 오류·루트 변경·대량 삭제는 보류합니다. 정상 삭제라면 BookOasis 수동 스캔 후 새 기준으로 시작하세요. 대기열은 중지해도 유지됩니다.
- 루트당 기본 20만·최대 120만 항목을 비교합니다. 실시간은 최초 수집 후 변경 범위를 비교하며 정기 전체 비교에는 비용이 발생합니다. Google Drive 경로는 기존 Drive 감지를 권장합니다.
- Mate의 로컬 처리에서는 rclone VFS 명령을 보내지 않습니다. BookOasis 보관함 자체의 스캔 전 갱신 설정은 별도로 적용될 수 있습니다.
- 상세 설정·안전 제한은 해당 메뉴 하단 매뉴얼을 참고하세요. 실제 SMB/NFS/FUSE 환경 검증은 배포 전 필요합니다.

## Changelog

- v1.11.3 (2026-09-13)
  - 로컬 감시 기준을 별도 SQLite에 저장하고 이어서 시작 지원. 활성화 상태 재시작 시 저장 기준과 전체 대조해 중지 중 변경을 수집하며 새 기준 시작은 명시적 초기화로 유지.
  - 조회 실패·설정 및 루트 식별 불일치 시 기준 보존. 이벤트 큐 저장 확인 후 기준 변경분 저장.
  - 상태를 조회 대기·최초 기준 수집·저장 기준 대조·감시 중으로 구분하고 미확인 개수는 0 대신 집계 중으로 표시.
  - 재시작 전체 확인과 inotify 재등록은 여전히 필요하며 큐/기준 저장 사이 종료 시 일부 이벤트 재전달 가능.

- v1.11.2 (2026-09-13)
  - 로컬 감시 경로당 최대 항목 한도를 100만에서 120만으로 확대. 기본값 20만은 유지하며 대상 파일과 폴더 합계로 집계.
  - 서버·감지 작업자 검증과 설정 화면·매뉴얼 한도를 함께 갱신. 기존 설정값은 유지하므로 작업 중지 후 한도를 변경·저장하고 새 기준으로 시작해야 적용.
  - 실시간 부분 비교 및 마운트·대량 삭제 보호 유지. OS inotify 한도는 별도이며 자동 변경하지 않음.

- v1.11.1 (2026-09-13)
  - 로컬 감시 항목 한도를 설정으로 제공. 경로당 기본 20만·최대 100만이며 대상 파일과 폴더 합계로 집계하고 상태에 각각 표시.
  - 실시간 감지를 최초 전체 수집 후 변경 파일·폴더 범위 비교로 개선. 단일 파일 갱신에서 전체 트리 탐색·전체 기준 복사를 제거.
  - 1시간마다 또는 변경 경로 10,000개 초과 시 전체 비교. 폴링은 전체 비교를 유지하며 제외 패턴·대상 확장자 필터 적용.
  - 저장 확인 후 기준 갱신, 실패 범위 재시도 및 마운트·대량 삭제 보호 유지. 최초·정기 전체 조회 비용과 OS inotify 한도는 별도이며 자동 변경하지 않음.

- v1.11.0 (2026-09-13)
  - 로컬 폴더 감지 메뉴 추가. Linux 로컬 실시간 감지와 Windows Docker·CIFS/NFS·FUSE 폴링, 독립 영속 큐·컨테이너 경로 매핑·마운트 및 대량 삭제 보호 지원.
  - 백그라운드 Flask 앱 컨텍스트 오류·중지 상태 보정. 최초/재시작은 새 기준만 수집하며 과거 변경을 자동 재수집하지 않음.
  - 로컬 설정·변경 이벤트·매뉴얼 탭과 Drive 스타일 UI, 경로 선택·예시, 제외 패턴, 로컬 전용 Discord 웹훅 추가.
  - watchdog 상태 확인·설치/재설치, 이벤트 JSON·종료 기록 삭제, 자동 삭제 On/Off·보관 기간(기본 30일), 정렬·DB·보관함·상태·변경 유형·건수 필터 추가.
  - Discord 알림에 상세 파일명·MIME·Drive ID·링크 및 정렬된 처리 요약 추가.
  - 플러그인 ZIP 드래그/클릭 선택 지원. .sh 기본 차단을 유지하되 신뢰 카탈로그 또는 직접 GitHub 입력의 명시 동의 시 파일 포함 허용. 스크립트 자동 실행 및 다른 위험 파일 검사 완화는 없음.
  - Linux 호스트 읽기 전용 대화형 진단과 Bash 한 줄 실행기 추가. 단계별 요약 화면·TXT 상세 근거·경로 예시·Enter 건너뛰기 및 민감정보 가림 지원.
  - 진단 URL은 http(s) 원점만 지원하며 하위 경로는 미지원. 정상 형식 주소 거절 제보는 원인 미확정으로 별도 확인 필요.

- v1.10.4 (2026-09-10)
  - Activity 휴지통 파일도 부모 경로가 감시 폴더 안으로 확인되면 삭제 전 경로로 활용. 파일 복구 작업은 수행하지 않음.
  - 개별 파일의 경로 복원 실패는 실패 목록에 보류하고 다음 활동 수집을 계속하도록 개선. 실패 행과 중복 처리 기록을 함께 저장하며 인증·호출 제한·서버 오류·DB 저장 실패의 체크포인트 보호는 유지.
  - 경로 확인 보류 항목에 파일 ID·동작·사유를 표시하고, 경로 수정 전 일반 재시도를 차단. 변경 이벤트 화면과 연동 매뉴얼 갱신.
  - 감시 범위 밖으로 이동한 파일의 이전 경로가 없으면 여전히 보류하며, 과거 기간 재수집이나 자동 경로 추정은 포함하지 않음.

- v1.10.3 (2026-09-10)
  - Activity API의 개별 action에 대상이 생략되고 공통 `targets[]`에만 포함된 변경 이벤트가 누락되던 문제 수정. 복수 대상 처리와 재조회 중복 방지를 유지.
  - 처리할 활동에 대상 정보가 없으면 오류를 기록하고 체크포인트를 보존해 조용한 누락 방지.
  - 배포 저장소에서 개발용 `tests/` 파일 제거. 로컬 검증 파일은 보존하며 기존 제외 규칙 유지.
  - 이미 누락된 과거 활동은 자동 복구하지 않음. 체크포인트 초기화는 과거 재수집이 아니라 현재 시각부터 다시 시작하므로 주의.

- v1.10.2 (2026-09-08)
  - 공통·라이브러리 진단·카테고리 이관·통DB 이관·Google Drive 연동 매뉴얼을 현재 기능과 처리 순서에 맞게 정리하고 중복 설명을 간소화. 기존 구조·처리 흐름 인포그래픽은 유지.
  - 설정 화면의 긴 연결·MariaDB·표지·Gitea 설명을 핵심 안내와 공통 매뉴얼 바로가기로 정리.
  - Gitea 연결 실패를 인증·사용자 조회 권한·저장소 권한·API 주소·요청 제한·서버/프록시·DNS·SSL·시간 초과·접속 오류로 구분하고, 연결 검사가 사용자 인증만 확인한다는 점을 명확화.

- v1.10.1 (2026-09-07)
  - 연동 설정·변경 이벤트 화면에 작업 중지 버튼 추가. 연동 사용을 끄고 현재 요청이 끝난 뒤 추가 수집·VFS·스캔 요청을 중단. 완료 결과는 보존하고 미처리 이벤트는 시도 횟수를 소모하지 않고 재시도 대기로 반환.
  - 중지 요청 중과 중지 상태를 구분해 표시. 중지 후 대기열 일괄 삭제가 가능하며, 재개는 연동 사용을 다시 켜고 저장. 이미 전달된 BookOasis 스캔은 취소하지 않으며 다중 대상 중 일부만 요청된 이벤트는 중복 방지를 위해 수동 확인 후 재시도.
  - 연동을 끄고 수집·스캔 배치가 종료된 상태에서 대기·재시도 이벤트를 일괄 삭제하는 기능 추가. 체크포인트·파일 비교 상태와 종료 이력은 보존.
  - 자체 Google Drive 감지에 경로별 Activity 방식을 추가. 기존 Changes 설정과 체크포인트를 보존하고 Activity 시간 구간·페이지 재개, 영속 중복 제거와 인증·호출 제한 재시도 보호를 적용.
  - Activity의 두 OAuth 권한, 재인증, 실제 대상 폴더, 수집 지연 및 경로 복원 제약을 설정 화면과 연동 매뉴얼에 안내.

- v1.10.0 (2026-09-07)
  - 카탈로그 원격 저장소 상태를 별도 확인하고 미설치 404 항목을 숨김. 설치 플러그인과 사용자 등록 원본은 보존하며 전체 새로고침에 원격 검증·Topic 검색을 연결.
  - MariaDB SQL 관리자 진단 모드, 시스템 테이블 허용 목록, 5개 운영 프리셋과 최대 5,000행·30초 제한을 추가. 스트리밍·읽기 전용 트랜잭션을 사용하며 민감 컬럼·위험 함수·쓰기 차단 유지.
  - SQL 진단 도구의 DB·프리셋 선택 너비를 조정하고, `PROCESS` 권한이 없는 MariaDB 계정에서 InnoDB 트랜잭션 진단이 실패하면 필요한 권한과 대체 진단 방법을 안내.
  - 연결 테스트에 원격 DB 엔진 교차 확인과 커버 경로 대응/이동 상태를 추가. 확인된 엔진 불일치 및 커버 이동·경로 확인 불일치는 관련 이관·커버 작업을 차단.
  - 동기 경로 스캔의 시간 초과·게이트웨이 오류는 완료 여부 불명으로 기록하고 자동 재시도를 중단해 중복 실행을 방지. 실패에도 보관함·VFS·스캔 상세 결과를 보존.
  - 관리자 경로 스캔을 우선 사용하고 404를 이유로 전체 스캔을 추가 요청하던 동작을 제거. 루트 지정이 불가능한 다중 루트 보관함은 해당 보관함 증분 큐로 처리.
  - 자체 Google Drive 감지에서 같은 경로의 수정시각·크기·MD5가 같으면 EDIT를 생략하고, 이벤트와 비교 상태를 함께 저장해 장애 후 페이지 재생 중복을 방지.
  - 감시 경로별 한 페이지(최대 1,000건) 처리와 체크포인트 저장, 호출 제한 시 지수 백오프 및 파일 상태 조회 인덱스를 적용.
  - 기존 비교 정보가 없는 파일의 최초 보수적 처리와 기존 대기열이 자동 정리되지 않는 점을 연동 매뉴얼에 안내.

- v1.9.11 (2026-09-03)
  - ZIP·GitHub·Gitea 패키지 검사 후 실제 설치·업데이트가 완료돼도 검사 전용 상태가 남아 상단 작업 영역과 설치 버튼이 계속 표시되던 문제를 수정. 검사 대기 화면은 유지하고 설치 성공 시 기존 5초 자동 숨김을 적용.
  - 저장소 설치·ZIP 설치 카드의 900px 너비 제한을 제거하고 검사 버튼을 Mate 공통 작은 버튼 규격으로 통일.

- v1.9.10 (2026-09-01)
  - 플러그인 설치·업데이트 작업 상태를 메뉴 상단에 표시하고, 실제 설치 성공만 자동으로 숨기며 검사·실패·중지 결과는 확인할 수 있도록 유지.
  - 설치 플러그인과 미설치 카탈로그를 하나의 현황 화면으로 통합하고 전체·활성화·비활성화·설치됨·미설치 집계 필터와 일관된 카드·버전 표시를 추가.
  - BookOasis 커스텀 설정 UI와 Mate 스키마 설정을 구분해 부분 설정 저장을 방지하고, 스키마 폼의 필수값·비밀값·체크박스·저장 상태와 접근성을 개선.
  - 다수 플러그인의 UI 번들이 포함된 관리 API 응답이 2MiB를 넘을 때 실행 상태 조회가 실패하던 문제를 관리 목록 전용 16MiB 제한으로 수정하고 다른 API의 기존 제한은 유지.

- v1.9.9 (2026-09-01)
  - Mate 전용 rclone 실행 파일과 rclone.conf를 직접 지정하고, 두 값을 비우면 기존 FF rclone 설정을 사용하는 선택적 폴백을 지원.
  - 선택한 rclone 버전과 rclone.conf 설정을 관리자 팝업에서 확인하고 설정 파일 경로·저장 제약을 연동 매뉴얼에 반영.
  - Google Drive API 401 또는 만료 토큰 발생 시 OAuth 토큰을 실제 갱신해 rclone.conf에 저장한 뒤 한 번만 재시도하고, 재연동이 필요한 `invalid_grant`를 구분해 안내.
  - rclone 실행 파일 행의 작업 버튼을 `파일 선택`, `버전 확인` 순서로 정렬.

- v1.9.8 (2026-08-29)
  - Kavita에서 여러 보관함에 같은 최종 경로로 등록된 도서를 BookOasis 도서 1권으로 안전하게 병합하고, 보관함 선택 근거와 중복 분석을 미리보기·결과에 표시.
  - 카테고리 이관과 통DB 이관 설정의 라벨 굵기, 입력창·경로 선택 버튼 크기와 정렬을 Mate 기본 설정 화면 형식으로 통일.
  - 전체 구성, Google Drive 변경 감지와 통DB 이관 흐름을 설명하는 반응형 정적 구조도를 관련 매뉴얼에 추가.

- v1.9.7 (2026-08-27)
  - Mate 자체 변경 감지에서 rclone Union 리모트의 직접 Google Drive upstream을 선택해 감시하도록 지원.
  - Union과 실제 Drive 조합별로 체크포인트와 파일 상태를 분리하고 기존 직접 Drive 설정과의 호환성을 유지.
  - 중첩 Union·crypt·로컬 upstream을 자동 선택하지 않도록 지원 범위와 설정 안내를 명확히 표시.

- v1.9.6 (2026-08-27)
  - 전체 매뉴얼에 일반·성인·오디오북·비디오 DB별 지원 범위와 실제 메뉴 위치를 반영.
  - 라이브러리 진단, 카테고리 이관과 통DB 이관의 엔진 조건·백업·가상 그룹·교체 범위를 명확히 정리.
  - 지원표와 기능 요약 카드의 공통 스타일 및 다크 모드 표시를 개선.

- v1.9.5 (2026-08-27)
  - Google Drive 변경 감지를 기존 CommandDispatcher, WebhookDispatcher, FF rclone 기반 Mate 자체 감지 중에서 선택하도록 확장.
  - Mate 자체 감지에 다중 Google Drive 경로, 지연 초기화, 공유 드라이브, 체크포인트 상태와 인증·타임아웃 재시도를 추가.
  - 변경 이벤트의 개별 경로 스캔·VFS 중복 방지와 실패 경로 수정·같은 접두사 일괄 재시도·보관함 재등록 후 일괄 재분류를 지원.
  - Google Drive 설정·상태·연동 매뉴얼을 정리하고 스캔 방식, OAuth 범위와 체크포인트 동작을 명확히 표시.

- v1.9.4 (2026-08-26)
  - 상태 요약과 라이브러리 통계를 FlaskFarm 웹 프로세스 밖의 전용 작업 프로세스로 분리.
  - 프로세스 간 중복 실행 잠금, 작업 내부 DB 연결 생성, watchdog과 통계 대기 제한으로 무한 대기 방지.

- v1.9.3 (2026-08-24)
  - MariaDB의 DB별 읽기 연결 풀 크기를 설정에서 1~64 범위로 조절하도록 추가.
  - 기본 풀 크기를 15로 조정하고 설정 저장 후 다음 DB 조회부터 새 크기를 적용.

- v1.9.2 (2026-08-24)
  - gevent 환경의 상태 요약·라이브러리 통계 내부 병렬 작업이 멈출 수 있는 문제 수정.
  - GitHub 릴리스 조회의 DNS·네트워크 지연이 FlaskFarm 응답을 차단하지 않도록 격리.

- v1.9.1 (2026-08-23)
  - Docker Compose 호환성과 환경 파일 편집 개선
    - BookOasis 공식 기본 Compose·GHCR·Build·MariaDB 변형과 Override 조합을 실행 중 컨테이너 label 우선으로 안전하게 선택하고, 저장된 기본 Compose/Override를 환경 파일 편집기에 자동 로드하도록 개선.
    - Compose가 stderr에 `version is obsolete` 같은 경고를 출력해도 JSON·컨테이너 ID 판별이 실패하지 않도록 stdout/stderr를 분리하고, 실시간 Docker 작업 로그는 기존처럼 통합 출력하도록 유지.
    - `Compose Override = 사용 안 함` 설정은 편집기를 비활성화하고, Compose 목록 갱신 완료 후 저장된 파일을 불러오도록 비동기 순서를 고정.
  - Docker 호스트 호환성과 설정 UI 개선
    - `docker compose` 플러그인을 찾지 못하는 호스트에서도 표준 경로의 Docker Compose v2 실행파일을 추가 탐지하고, Compose v1만 있는 경우 v2 필요 조건과 실제 탐지 오류를 명확히 표시.
    - BookOasis Docker 경로의 입력창·경로 선택 버튼·Compose 선택창을 FlaskFarm의 작은 컨트롤 높이에 맞춰 정렬하고 버튼이 다음 줄로 내려가는 문제를 수정.
    - Docker 관리 기능에 FlaskFarm 호스트 루트 `/:/host` 마운트 필수 조건과 Compose 설정 예시를 추가.

- v1.9.0 (2026-08-22)
  - BookOasis Docker 운영 기능 추가
    - `.env`, 기본 Docker Compose와 Compose Override를 설정에서 백업 후 안전하게 편집하도록 추가.
    - `bookoasis` 서비스만 대상으로 재시작·구성 적용·이미지 업데이트·소스 Build 업데이트를 실행하고 `--no-deps`와 대상 컨테이너 검증을 강제.
    - GHCR Registry API로 현재 이미지와 `stable` digest·버전을 비교해 실제 업데이트가 있을 때만 이미지 업데이트를 허용.
    - Docker CLI·Compose·Git 경로와 `linux/amd64`·`linux/arm64` 플랫폼을 자동 탐지해 Ubuntu와 Synology Container Manager 환경을 함께 지원.
    - Docker 작업 stdout/stderr를 제한된 tail 버퍼로 실시간 표시해 pull·build·재생성 진행 상태를 작업 중 바로 확인.
    - BookOasis 공식 `docker-compose.yml`·`build`·`ghcr`·`mariadb`·`mariadb.ghcr` 변형과 generic/MariaDB Override를 자동 탐지하고, 여러 파일이 있으면 설정에서 기본 Compose와 Override를 직접 선택하도록 지원.
  - 설정 및 배포 UX 개선
    - MariaDB 필수 구성요소 설치 UI를 `설정 → DB 연결`로 이동하고 이미 사용 가능한 패키지는 건드리지 않는 `필요 패키지 설치` 흐름 추가.
    - DB 연결 영역을 최대폭으로 제한하고 일반·성인·오디오북·비디오 DB 카드를 데스크톱 4열, 중간 화면 2열, 모바일 1열로 정리.
    - Mate 현재 버전·GitHub 업데이트 여부와 README 기반 Changelog를 설정/매뉴얼 화면에서 확인하도록 추가.
  - 보안·안정성 강화
    - 읽기 전용 SQL에서 `INTO OUTFILE`·`DUMPFILE`을 차단하고 민감 임시 설정 파일 권한, HTTP 응답 크기와 Docker 명령 출력 메모리 상한을 강화.
    - `.env` 백업을 제한 권한·최근 5개 보관으로 정리하고 GitHub 탐색의 일시적 408/5xx 재시도와 진단 추적 정보를 추가.
    - 플러그인 자동 업데이트 스케줄러의 Flask application context 오류를 수정하고 Synology의 `/volume*` 호스트 경로를 `/host` 마운트 아래로 안전하게 매핑.

- v1.8.2 (2026-08-21)
  - 플러그인 관리 설정에서 경로 선택 버튼을 누르면 폼이 제출되어 다른 탭으로 이동하던 문제 수정.

- v1.8.1 (2026-08-21)
  - GitHub에서 비공개 Gitea로 이전한 플러그인이 폐기된 GitHub 업데이트 주소를 호출하던 문제 수정.
  - 플러그인 상태 새로고침 시 로드 상태가 미확인으로 표시되는 문제 수정.
  - 플러그인 자동 업데이트 확인 예약 설정이 저장되지 않던 문제 수정.

- v1.8.0 (2026-08-21)
  - 일반·성인·오디오북에 이어 비디오 DB의 상태 요약, 라이브러리 통계, 스캔, Google Drive 연동과 이관을 지원.
  - MariaDB 대형 라이브러리 집계를 백그라운드·캐시 기반으로 개선하고 서버 설정과 ISBN 인덱스 진단을 추가.
  - 카테고리 가져오기에서 DB 종류가 다른 동일 ID 표지 충돌을 비파괴적으로 처리.
  - 여러 비공개 Gitea 서버의 카탈로그 탐색·설치와 플러그인 업데이트 확인 상태·예약·UI를 개선.

- v1.7.1 (2026-08-14)
  - MariaDB 대형 라이브러리 통계 성능 개선
    - 전체 행을 한 번에 메모리에 적재하지 않고 스트리밍·배치 집계하며 문자열 정규화와 중복 계산을 줄임.
    - 라이브러리 통계를 백그라운드에서 계산하고 최근 결과를 즉시 복원하는 영속 캐시와 지연 렌더링을 추가.
    - 동일 조건의 실행 중 작업과 최근 결과를 재사용해 화면 재진입·중복 요청으로 인한 DB 부하를 완화.
  - 상태 요약 응답성 개선
    - FF 재시작 후에도 최근 상태 요약을 우선 표시하는 영속 스냅샷과 원본 지문 검증을 추가.
    - 일반·성인·오디오북 DB 점검을 제한된 병렬 작업으로 수행하고 같은 대상의 중복 점검을 합침.
    - 대형 DB의 자동 중복 ISBN 전체 집계를 제외하고 필요 시 수동 진단에서 확인하도록 변경.

- v1.7.0 (2026-08-14)
  - BookOasis 플러그인 관리 개선
    - BookOasis 플러그인 관리 API를 이용해 활성화·로드 상태·설정 스키마를 설치 플러그인 카드에 통합.
    - GitHub Topic 탐색 결과를 검증 카탈로그와 같은 목록으로 제공하고, 설치 시 안전 패키지 검사를 거쳐 적용하도록 개선.
    - 설치 플러그인 메뉴 진입 시 캐시 기반으로 원격 버전을 확인하고 실제 새 버전이 있을 때만 업데이트 버튼을 표시.
    - 설치 플러그인 카드에 현재 버전과 GitHub 저장소 버튼을 추가하고 카탈로그의 중복 검사 버튼을 정리.
    - `my_reading_summary` 카탈로그 추가와 원격 Provider 이름 동기화 지원.
  - Google Drive 변경 감지 연동 강화
    - 최신 BookOasis 개별 경로 스캔 API를 우선 사용하고 구버전 관리자 API와 전체 보관함 스캔을 단계적으로 폴백.
    - 동일 배치의 VFS 갱신·부분 스캔 요청을 중복 제거하고 선택 DB 연결 실패를 다른 DB 처리와 격리.
    - 변경 이벤트를 도서 제목·상세 경로·액션·수신 시각이 포함된 Discord 카드 메시지로 묶어 발송.
  - MariaDB 대형 라이브러리 안정성 개선
    - 문제 도서 전체 건수 집계 제한 시간 초과 시 최근 집계 또는 현재 페이지 결과를 유지하도록 개선.
    - 라이브러리 통계의 고비용 문자열 `COUNT(DISTINCT ...)`를 제거하고 기존 배치 순회 결과를 재사용.
    - 중복 ISBN·시리즈 누락·문제 도서 재스캔용 조회를 요청 범위에 맞게 분리하고 결과 캐시와 키셋 배치를 적용.

- v1.6.5 (2026-08-11)
  - BookOasis 플러그인 관리자 추가
    - 검증 카탈로그를 카드 형태로 제공하고 GitHub 저장소 또는 ZIP 파일로 네이티브 플러그인을 검사한 뒤 설치·업데이트하는 기능 추가.
    - 사용자가 GitHub 주소와 소개를 카탈로그에 등록하고 사용자·검증 분류를 선택할 수 있는 기능 추가.
    - 설치 위치·압축 구조·플러그인 ID·버전·필수 의존성·중첩 압축을 검사하고 기존 플러그인을 백업한 뒤 원자적으로 교체하도록 구성.
    - 설치된 플러그인을 확인 절차와 함께 삭제하는 기능 추가.
    - Activity, Activity Desk, 독서 통계, 네이버·카카오·리디 검색, ISBN 추출, 통합 도서 검색, Pixiv 랭킹, 플러그인 매니저, 플러그인 게시판과 소설넷 평점 플러그인을 검증 카탈로그에 등록.
  - 과거 ZIP 로그 정리 기능 추가
    - BookOasis 로그 디렉터리의 과거 ZIP 파일을 조회하고 개별 삭제하는 기능 추가.
    - ZIP 파일 수와 용량을 확인한 뒤 한 번에 삭제하는 기능을 추가하고 현재 로그·하위 폴더·심볼릭 링크는 삭제 대상에서 제외.

- v1.6.4 (2026-08-10)
  - MariaDB 필수 패키지 설치 지원
    - 공통 매뉴얼에서 FF 컨테이너의 PyMySQL과 mariadb-client 설치 상태·버전을 확인하는 기능 추가.
    - PyMySQL과 mariadb-client를 개별 또는 일괄로 백그라운드 설치하고 진행률·결과 출력을 확인하는 기능 추가.
    - 설치 대상을 고정 목록으로 제한하고 실행 전 확인, root 권한과 apt-get·apk 지원 여부 검사를 적용.
  - 공통 매뉴얼 가독성 개선
    - MariaDB 필수 패키지 명령과 사용자 이미지 예시를 Bash·Dockerfile 언어 라벨이 있는 코드 창으로 정리.
    - 컨테이너 재생성 시 직접 설치한 패키지가 사라질 수 있다는 유지 관리 안내 보강.

- v1.6.3 (2026-08-10)
  - Google Drive 경로 매핑 진단 개선
    - 설정 화면에서 gd-poller 수신 경로를 입력해 Mate 변환 경로, BookOasis 보관함과 rclone VFS 규칙을 저장 전에 확인하는 기능 추가.
    - `/mnt/gds2/mnt/gds2/...` 형태의 중복 접두사를 감지하고 gd-poller `target`·`mappings` 중복 설정과 기존 실패 이벤트 처리 방법을 구체적으로 안내.
    - 정상적인 반복 디렉터리를 보호하기 위해 경로를 임의로 자동 보정하지 않고, 검사 결과에 rclone 인증정보가 노출되지 않도록 처리.
  - gd-poller 연동 매뉴얼 개선
    - 전체 `config.yaml` 예시와 gd-poller·Mate 중 한 곳에서만 경로를 변환하는 원칙 추가.
    - 공식 gd-poller GitHub 기반 설치·업데이트·실행 명령을 언어 라벨이 있는 코드 창으로 정리.

- v1.6.2 (2026-08-10)
  - 설정 및 조회 UX 개선
    - 설정에서 BookOasis 루트의 `.env`를 안전하게 읽고, 기존 파일을 백업한 뒤 저장할 수 있는 편집 기능 추가.
    - 문제 도서와 검사 이력 조회에 실행 상태·경과 시간·중복 요청 방지를 추가하고, 문제 도서는 조회 버튼이나 Enter 입력으로만 실행하도록 변경.
  - MariaDB 조회 성능 개선
    - 읽기 전용 연결을 DB별 최대 4개까지 재사용하고 쓰기·이관 연결은 독립 연결로 유지.
    - 테이블·컬럼 스키마를 5분, 동일 문제 도서 조회 결과를 15초간 캐시하며 설정 및 도서 변경 작업 후 관련 캐시 초기화.
    - 문제 도서 목록과 전체 건수를 한 번의 조회로 집계해 중복 전체 순회 제거.

- v1.6.1 (2026-08-09)
  - 상태 요약 차트 안정화
    - 이전 버전 CSS가 브라우저에 남아 있을 때 Chart.js 도넛 차트가 화면 전체로 확대되는 문제 수정.
    - 상태 요약 CSS 캐시를 자동 갱신하고 CSS 로딩 상태와 무관하게 차트 크기를 176px로 제한.

- v1.6.0 (2026-08-09)
  - 설정·메뉴 및 진단 UI 개선
    - 설정을 연결·DB 엔진·경로·스캔·진단 탭으로 구분하고 공통 매뉴얼과 커스텀 폰트를 독립 메뉴로 정리.
    - 상태 요약을 Chart.js 기반 건강도 대시보드로 재구성하고 캐시 우선 표시와 백그라운드 갱신으로 MariaDB 첫 화면 응답 개선.
    - 일반·성인·오디오북의 포맷·저장 용량·보관함·장르·태그·메타데이터 완성도·등록 추이를 제공하는 라이브러리 통계 추가.
  - MariaDB·오디오북 운영 진단 강화
    - SQL 도구 프리셋을 SQLite·MariaDB 공통 문법으로 정리하고 오디오북 최근 재생·고아 진행률·보관함 집계 진단 추가.
    - 오디오북 점검 필요 수치의 포스터·저자·출판사·소개·트랙 수 누락 근거와 중복 제거 기준 표시.
    - 공통 구현 참고와 엔진별 필수 패키지 안내를 최상단 매뉴얼로 이동하고 기능별 이관 범위를 각 매뉴얼에 분리.
  - 대형 카테고리 패키지 안정성
    - `manifest.json`과 `metadata.json` 제한을 분리하고 최대 2GB metadata 패키지 지원.
    - 64MB 이상 metadata의 패키지 검사는 전체 JSON 대신 manifest와 카테고리 정보만 읽어 FF 웹 프로세스의 메모리 사용 억제.
    - 새 패키지 manifest에 offset 수를 기록하고 기존 대형 패키지는 가져오기 작업 중 상세 수량을 집계하도록 표시.

- v1.5.2 (2026-08-09)
  - UI·매뉴얼 설명 일치화
    - Kavita 이관의 SQLite·MariaDB 사전 백업 방식과 일반·성인 대상 범위를 현재 구현에 맞게 정리.
    - 라이브러리 무결성 검사, Google Drive DB 설정과 오디오북 카테고리·통DB 이관 안내의 오래된 표현 수정.
    - MariaDB 계정 권한과 쓰기 제한 시간 설명을 진단·이관 기능의 실제 사용 범위에 맞게 수정.

- v1.5.1 (2026-08-09)
  - 통DB 이관 안내 정비
    - SQLite·MariaDB 통DB 내보내기·가져오기와 실행 전 백업의 오디오북 포함 범위를 실제 동작에 맞게 표시.
    - FF 컨테이너의 PyMySQL·mariadb-client 임시 설치, 검증과 사용자 이미지 영구 설치 방법을 이관 매뉴얼에 추가.

- v1.5.0 (2026-08-09)
  - 오디오북 카테고리 이관
    - 공식 2.0 패키지 계약으로 오디오북·트랙·재생 진행률·트랙 진행률과 포스터 내보내기 지원.
    - SQLite·MariaDB 오디오북 DB로 신규 카테고리 가져오기와 기존 카테고리 병합 지원.
    - 다중 물리 경로 재매핑, 사용자 ID 검증, 중복 폴더·트랙 건너뛰기와 트랜잭션 롤백 적용.
  - 통DB 오디오북 표시
    - SQLite 패키지의 선택적 오디오북 DB와 MariaDB 패키지의 오디오북 DB 포함 상태를 미리보기·작업 결과에 표시.

- v1.4.0 (2026-08-09)
  - MariaDB 이관 지원
    - 카테고리 내보내기·가져오기·병합을 공통 쓰기 어댑터와 트랜잭션으로 전환.
    - Kavita → BookOasis 이관의 대상 DB로 MariaDB 지원 추가.
    - 일반·성인·오디오북 DB를 `mariadb-dump` 논리 패키지로 내보내고 동일 엔진에 복원하는 통DB 이관 추가.
  - 안전성 및 호환성
    - MariaDB 통DB 패키지 manifest의 파일 크기와 SHA-256을 검사하고 실제 복원 전 논리 백업을 강제.
    - 복원·경로 변경·무결성 검사 실패 시 기존 MariaDB 논리 백업으로 롤백.
    - 기존 SQLite 2개 DB 통DB 패키지를 계속 지원하며 오디오북 DB가 있는 새 패키지는 함께 처리.
    - DB 자격증명을 명령줄에 노출하지 않고 권한 제한 임시 client 설정으로 전달.

- v1.3.0 (2026-08-08)
  - SQLite·MariaDB 이중 엔진 지원
    - 설정에서 DB 엔진을 명시적으로 선택하거나 BookOasis 환경변수를 기준으로 자동 감지하도록 추가.
    - 일반·성인·오디오북 진단, 보관함 조회, 스캔 상태, 시리즈 누락과 표지 참조 조회를 공통 읽기 전용 DB 어댑터로 전환.
    - MariaDB의 `%s` placeholder, dict row, `utf8mb4_bin` 환경과 날짜·Decimal 결과 정규화 반영.
  - 운영 안전성
    - MariaDB 연결 실패 시 오래된 SQLite 파일로 조용히 전환하지 않도록 원본 엔진을 명확히 표시.
    - 표지 검사, 고아 표지 정리와 일괄 재스캔 작업의 MariaDB 설정 파일을 일회성·제한 권한으로 처리.
    - SQL 도구에 MariaDB 제한 시간과 민감 테이블·시스템 스키마 차단 정책 적용.
    - SQLite 파일 교체 방식의 카테고리·통DB 이관을 MariaDB에서 차단하고 공식 도구 안내 추가.

- v1.2.0 (2026-08-04)
  - 오디오북 대응
    - `media_audiobook.db` 상태 요약, DB 정보, 읽기 전용 SQL과 스캔 상태 지원.
    - Google Drive 변경 감지에서 오디오 파일과 오디오북 보관함 경로 인식 지원.
    - 도서 전용 진단·메타데이터 작업에는 오디오북을 노출하지 않도록 범위 분리.
  - 커스텀 폰트
    - BookOasis 커스텀 폰트 디렉터리에 TTF·OTF·WOFF·WOFF2 파일을 안전하게 업로드하는 설정 UI 추가.
    - 허용 형식·파일 헤더·경로를 검증하고 설치 목록과 도커 재시작 안내 제공.
  - 상세 보기
    - BookOasis v1.6.4의 표준 난독화 딥링크와 레거시 폴백 계약 반영.
    - 일반·성인 DB 모두 상세 페이지를 새 탭에서 복원하도록 이전 차단 정책 제거.
  - 배포 구성
    - 공개 저장소에서 로컬 테스트 소스와 개발 기록을 제외하도록 정리.

- v1.1.0 (2026-08-03)
  - BookOasis 연동
    - 최신 관리자 API의 플러그인 상태·업데이트 정보와 개별 도서 작업 계약 반영.
    - 난독화된 도서 상세 주소 체계 지원과 상세 보기 호환성 개선.
    - 최신 스캐너 상태, 원격 도서 페이지 분석 제한과 gd-poller 변경 감지 기준 반영.
  - 라이브러리 진단
    - 일반·성인 DB를 안전하게 조회하는 읽기 전용 SQL 도구와 사용자·권한·진행률 진단 프리셋 추가.
    - 스캔 상태 표시와 장기 미스캔·원격 보관함 진단 기준 개선.
  - 도서 재스캔
    - 문제 도서와 표지 검사 결과의 백그라운드 일괄 재스캔 추가.
    - 일시적인 API 연결 중단을 항목별 재시도하고 화면별 작업 상태를 분리해 표시하도록 안정성 개선.
    - 결과 행 메뉴의 개별 도서 재스캔 복원과 일괄 작업 중 중복 실행 방지.
  - 표지 검사
    - 저해상도·작은 파일·비정상 비율 검사를 웹 요청과 분리된 백그라운드 작업으로 전환.
    - 한 번의 파일 분석 결과를 세 검사 유형이 공유하고 메뉴 재진입 시 진행률과 결과를 복원하도록 개선.
    - 표지 없음 검사는 빠른 DB 조회 방식을 유지하고 정밀 검사 결과를 일괄 재스캔과 연동.
- v1.0.2 (2026-07-29)
  - 고아 표지파일 정리와 카테고리 이관을 별도 프로세스로 분리해 장시간 작업 중 FF 응답성을 개선.
  - BookOasis URL, 스캔 웹훅 토큰과 API 제한 시간 안내를 보완.
  - 커스텀 표지 디렉터리 설정과 경로 선택 기능 추가.
  - 기존 카테고리 병합 시 불필요한 신규 카테고리 이름 입력 제거.
- v1.0.1 (2026-07-29)
  - 통DB 이관 매뉴얼의 Kavita 이관 절차를 현재 동작에 맞게 수정.
  - README 플러그인 설명을 FF 표기로 정리.
- v1.0.0 (2026-07-29)
  - 최초 공개.
